import { NavLink } from "react-router-dom";

export default function Users() {
  return (
    <div>
      <nav>
        <NavLink to="/">Back to login</NavLink>
      </nav>
      <h2>Users</h2>
    </div>
  );
}
